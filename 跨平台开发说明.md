# 码伴 PixelPal 跨平台开发说明

## 当前状态

已经完成 Electron 跨平台桌面壳：

- 透明窗口
- 无边框
- 置顶
- 前端核心逻辑
- 打开网站桥接
- 打开本地应用桥接
- 系统托盘菜单
- Windows/macOS 前台应用识别接口
- DeepSeek Key 加密保存接口
- Windows/macOS 打包配置雏形

## 本机当前状态

Electron 运行时已可启动，本机已生成 macOS Beta zip。

当前本机主要限制是：这里不是 Windows 环境，不能直接验证 Windows 安装包。Windows 包建议在 Windows 电脑或 GitHub Actions 中生成。

## 启动方式

网络可用后，先运行：

```bash
./install-electron-runtime.command
```

然后运行：

```bash
./start-electron-local.command
```

或：

```bash
npm run electron:local
```

## 打包方式

macOS / Windows 通用命令：

```bash
npm run pack
```

macOS Beta：

```bash
npm run pack:mac
```

Windows Beta：

```bash
npm run pack:win
```

GitHub 自动打包：

- `.github/workflows/build-beta.yml`

上传到 GitHub 后，在 Actions 页面运行 `Build PixelPal Beta`。

## 下一步

1. 用 GitHub Actions 生成 Windows 安装包。
2. 进行双平台内测。
3. 补正式图标。
4. 做 macOS / Windows 正式签名。
5. 准备公开下载页和反馈入口。

## 当前已接入的平台桥接

前端可以通过 `window.pixelpal` 调用：

- `openUrl(url)`：打开网页
- `openApp(target)`：打开本地应用
- `activeContext()`：获取当前前台应用/窗口信息
- `platform()`：获取平台信息
- `saveSecret(key, value)`：加密保存密钥
- `loadSecret(key)`：读取密钥
- `close()`：关闭应用
