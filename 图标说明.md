# 码伴 PixelPal 图标说明

当前已生成：

- `build/icon.png`
- `build/icon.icns`
- `build/icon.ico`

图标是原创像素风：暖白色编程小伙伴、深色屏幕脸、小眼镜、键盘和顶部小气泡。

当前 Electron Builder 已配置使用：

- macOS：`build/icon.icns`
- Windows：`build/icon.ico`
- 通用源图：`build/icon.png`

图标由 `scripts/generate-icon.mjs` 生成，不依赖设计软件。脚本会同时生成 PNG、macOS ICNS、Windows ICO 和 iconset PNG。

正式发布前建议继续检查：

1. macOS Dock 中是否清晰。
2. Windows 桌面快捷方式是否清晰。
3. 小尺寸 16x16 / 32x32 是否能辨认。
4. 如果未来找设计师重绘，仍然放到 `build/icon.icns`、`build/icon.ico`、`build/icon.png`。
