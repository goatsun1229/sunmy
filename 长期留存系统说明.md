# 码伴 PixelPal 长期留存系统说明

## 技术栈总结

- Electron 桌面壳。
- React + Vite 渲染层。
- Electron 主进程负责窗口、托盘、系统应用打开、前台应用识别、安全存储。
- 用户成长数据默认保存在本机 `localStorage`。
- DeepSeek Key 继续使用 Electron `safeStorage` 保存到 `userData/secrets.json`。

## 新增模块

- `src/pet-system/types.ts`：统一数据类型。
- `src/pet-system/config.ts`：每日任务、收藏、成就、随机事件、活动和商品配置。
- `src/pet-system/dialogues.ts`：100 条情境台词。
- `src/pet-system/storage.ts`：创建默认数据、迁移、备份、导入导出、每日 rollover。
- `src/pet-system/engine.ts`：关系成长、回忆、成就、随机事件、七日纪念卡。
- `src/pet-system/commerce.ts`：PaymentProvider、MockPaymentProvider、EntitlementService 基础骨架。

## 数据结构

主存档键：

- `pixelpal.companion.v1`

备份键：

- `pixelpal.companion.backup`
- 导入前备份：`pixelpal.companion.backup.<timestamp>`

主要结构：

- `profile`：petId、petName、ownerName、firstMeetDate、totalActiveMinutes、consecutiveActiveDays、currentVersion、dataSchemaVersion。
- `relationship`：familiarity、trust、bond、interactionCount、提醒完成/忽略、sharedMemoryCount、每日增长限制。
- `personality`：active、quiet、clingy、independent、playful、caring。
- `state`：energy、mood、curiosity、security、roomComfort、dailyMoodLabel。
- `daily`：每日欢迎、每日状态、每日轻互动、当天分钟数和互动数。
- `memories`：回忆册时间线。
- `collectibles`：收藏和装饰。
- `achievements`：成就。
- `stats`：本地统计。
- `entitlements`：权益，带基础签名。
- `eventLog`：随机事件冷却和每日次数。
- `quietMode`：安静模式配置。

## 数据迁移

旧用户的 `pixelpal.settings` 和 `pixelpal.reminders` 不会被删除。

首次升级时：

1. 读取旧设置里的宠物名和主人名。
2. 创建或迁移 `pixelpal.companion.v1`。
3. 迁移前写入备份。
4. 损坏数据会回退到默认结构，不覆盖 DeepSeek Key。

## 七日养成流程

- 第一天：创建首次相遇回忆，记录宠物名和主人名。
- 第二天：每日问候会记住主人称呼。
- 第三天：可通过成就解锁免费装饰。
- 第四至第六天：通过每日状态、随机事件和台词逐步变化。
- 第七天：生成七日纪念回忆，可在回忆册保存纪念卡 SVG。

## 随机事件配置位置

`src/pet-system/config.ts` 的 `randomEvents`。

当前包含 30 个事件，并带有：

- 冷却时间；
- 每日触发上限；
- 关系要求；
- 性格要求；
- 动画 key；
- 台词 key；
- 奖励；
- 安全降级。

## 台词配置位置

`src/pet-system/dialogues.ts`

当前包含 100 条情境台词，按分类组织。

## 商品和权益配置位置

- 商品配置：`src/pet-system/config.ts` 的 `products`。
- 权益服务：`src/pet-system/commerce.ts`。
- 当前仅有 MockPaymentProvider 开发骨架，没有接入真实支付。

## 当前占位资源

收藏和回忆图片使用 `placeholder:*` 占位，不引用外部未知版权素材。

## 已知未完成

- 真实支付未接入。
- 云存档未接入。
- 独立小屋窗口未拆出，目前小屋为现有桌宠窗口内的独立面板。
- 七日纪念卡保存为 SVG 本地图片。
- 活动系统已有配置框架，复杂节日视觉素材未制作。
