# 码伴 PixelPal 0.6.0 视觉素材路线

## 本版目标

让安装包和桌面快捷方式更像一个正式产品。

## 已完成

- 生成 `build/icon.png`。
- 生成 `build/icon.icns`。
- 生成 `build/icon.ico`。
- Electron macOS 打包使用 `build/icon.icns`。
- Electron Windows 打包使用 `build/icon.ico`。
- 增加截图素材清单。

## 需要测试

- macOS Dock 图标是否清晰。
- Windows 桌面快捷方式是否显示图标。
- Windows 安装器是否显示图标。
- 小尺寸图标是否还能看出角色轮廓。

## 下一步

- 按 `截图素材清单.md` 准备应用截图素材。
- 做下载页首屏图。
- 整理 1.0 Beta 发布检查清单。
