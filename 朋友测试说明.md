# 码伴 PixelPal 朋友测试说明

## 现在可发的测试包

macOS Apple Silicon 当前已有：

- `release/码伴-PixelPal-macOS-arm64-Beta.zip`

适合先发给身边用 M 系列 Mac 的朋友测试。

如果重新打包时提示无法连接 GitHub，可以先继续使用这个已有 zip；那是打包工具下载资源失败，不是桌宠本身坏了。

## 朋友怎么打开

1. 解压 zip。
2. 双击 `码伴 PixelPal.app`。
3. 如果系统提示无法验证开发者，右键应用选择“打开”。

## 让朋友重点测什么

- 能不能正常显示在桌面上。
- 拖动、贴边、隐藏、右键退出是否正常。
- 打开 B 站、打开计算器、问天气这些简单指令是否可用。
- 使用剪映、Claude、Codex、浏览器看视频时，小机器人状态是否会变化。
- 首次设置是否顺手。
- 提醒面板是否能添加、删除提醒。
- 提醒气泡是否按时出现。
- DeepSeek Key 设置后，问答是否正常。
- 点击反馈按钮后，是否能复制反馈模板。

## Windows 测试包

Windows 安装包需要在 Windows 电脑或 GitHub 自动打包里生成。

本地 Windows 机器打包：

```bash
npm install
npm run pack:win
```

生成后把 `release` 里的 `.exe` 发给朋友测试。

## 自动打包

项目已经加入 GitHub Actions 自动打包文件：

- `.github/workflows/build-beta.yml`

把 `pixelpal-crossplatform` 这个文件夹上传到 GitHub 仓库后，可以在 GitHub 的 Actions 页面手动运行 `Build PixelPal Beta`。完成后会出现两个下载包：

- `PixelPal-macOS-Beta`
- `PixelPal-Windows-Beta`

## 测试反馈模板

请朋友尽量按这个格式反馈：

```text
电脑系统：
是否能打开：
是否能退出：
哪里不好用：
希望增加什么：
截图或录屏：
```
